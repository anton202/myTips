import { ExpandTableDirective } from './expand-table.directive';

describe('ExpandTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandTableDirective();
    expect(directive).toBeTruthy();
  });
});
