import { HighLightTipDirective } from './highlightTip.directive';

describe('HighlightTipDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightTipDirective();
    expect(directive).toBeTruthy();
  });
});
